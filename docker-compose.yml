version: "3.1"
services:
    db:
        image: postgres
        ports:
            - "5432:5432"
        environment:
            POSTGRES_PASSWORD: 12345678
            POSTGRES_USER: root
            POSTGRES_DB: pegueleve_db
        volumes:
            - ./docker/postgres:/var/lib/postgresql/data
